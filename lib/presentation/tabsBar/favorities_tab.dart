import 'package:flutter/material.dart';

class FavoritiesTab extends StatelessWidget {
  const FavoritiesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Center(child: Text("Favories"))));
  }
}
