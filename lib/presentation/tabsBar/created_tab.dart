import 'package:flutter/material.dart';

class CreatedTab extends StatelessWidget {
  const CreatedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Center(child: Text("Created"))));
  }
}
